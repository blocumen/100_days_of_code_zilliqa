Run following command to start the http server which will server index.html

For Python3:
python3 -m http.server

For Python <3:
python -m SimpleHTTPServer


Server will run at port 8000(check port no. in terminal in case it doesn't work) and could be accessed by browser at following link
http://localhost:8000/